# Provider configuration template
#
# Configure features and explicit tenant/subscription aliases here. Credentials
# come from GitHub OIDC environment variables and must never be Terraform inputs.
# Add Microsoft Graph/AzureAD providers only to roots that own directory objects.

