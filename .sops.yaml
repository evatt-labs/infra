# sops creation rules for evattlabs-infra/secrets/.
# Single key — helicon workstation age key.
# Rotate with: `sops updatekeys secrets/*.enc.yaml`

keys:
  - &helicon age1z9c9j8j3l8vjfa50s8dfe8xmtffp3sg8zyartekxc44qmjx8ldvqwzyucx

creation_rules:
  - path_regex: secrets/.*\.(yaml|json|env|ini)$
    key_groups:
      - age:
          - *helicon
